{
  "name": "workspace",
  "scripts": {
    "tauri": "tauri",
    "elm:build": "elm make src/Main.elm --output=./dist/elm.js",
    "elm:review": "elm-review",
    "elm:review:watch": "elm-review --watch",
    "elm:review:fix": "elm-review --fix",
    "start": "npm run elm:build && tauri dev",
    "check": "npm run elm:build && npm run elm:review"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.0.0-beta.10",
    "elm-format": "^0.8.5",
    "elm-review": "^2.6.1"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.0.0-beta.8",
    "elm": "^0.19.1-5"
  }
}
