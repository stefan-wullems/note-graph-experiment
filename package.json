{
  "name": "workspace",
  "scripts": {
    "tauri": "tauri",
    "start": "elm make src/Main.elm --output=./dist/index.html && tauri dev"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.0.0-beta.10"
  },
  "dependencies": {
    "@tauri-apps/api": "^1.0.0-beta.8",
    "@webpack-cli/serve": "^1.6.0"
  }
}